CREATE TABLE client_goods_icon (id int(15) NOT NULL,
                name varchar(255) NOT NULL,
                PRIMARY KEY (id),
                KEY `name` (`name`)
                ) ENGINE=InnoDB DEFAULT CHARSET=utf8;